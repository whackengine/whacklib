package
{
    /**
     * Compile-time special type used for indicating that a variable
     * holds a JavaScript value.
     *
     * When communicating with the JavaScript host, this type generates
     * faster JavaScript operators when using ActionScript 3 operators.
     */
    public dynamic final class JSVal
    {
    }
}